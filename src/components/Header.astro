---
const navLinks = [
  { href: '/', label: 'Accueil' },
  { href: '/methode-pilates', label: 'La méthode' },
  { href: '/a-propos', label: 'À propos' },
  { href: '/salle-1', label: 'Salle 1' },
  { href: '/salle-2', label: 'Salle 2' },
  { href: '/contact', label: 'Contact' },
];

const currentPath = new URL(Astro.request.url).pathname;
---

<header class="site-header">
  <div class="container site-header__inner">
    <a href="/" class="site-header__brand">Ophélie Ros</a>
    <nav aria-label="Navigation principale">
      <ul class="site-header__nav">
        {navLinks.map((link) => (
          <li>
            <a
              href={link.href}
              aria-current={currentPath === link.href ? 'page' : undefined}
            >
              {link.label}
            </a>
          </li>
        ))}
      </ul>
    </nav>
    <a href="/reservation" class="btn btn--primary site-header__cta">Réserver</a>
  </div>
</header>

<style>
  .site-header {
    background: var(--color-cream);
    border-bottom: 1px solid var(--color-border);
    position: sticky;
    top: 0;
    z-index: 50;
  }

  .site-header__inner {
    display: flex;
    align-items: center;
    justify-content: space-between;
    gap: 1.5rem;
    padding-top: 1.1rem;
    padding-bottom: 1.1rem;
    flex-wrap: wrap;
  }

  .site-header__brand {
    font-family: var(--font-heading);
    font-size: 1.3rem;
    font-weight: 600;
    color: var(--color-ink);
  }

  .site-header__brand:hover {
    text-decoration: none;
    color: var(--color-sage-dark);
  }

  .site-header__nav {
    display: flex;
    list-style: none;
    gap: 1.6rem;
    margin: 0;
    padding: 0;
    flex-wrap: wrap;
  }

  .site-header__nav a {
    color: var(--color-ink-soft);
    font-size: 0.95rem;
    font-weight: 500;
  }

  .site-header__nav a[aria-current='page'] {
    color: var(--color-terracotta);
  }

  .site-header__cta {
    padding: 0.6rem 1.3rem;
    font-size: 0.9rem;
  }

  @media (max-width: 640px) {
    .site-header__nav {
      gap: 1rem;
      order: 3;
      width: 100%;
    }
  }
</style>
